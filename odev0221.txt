Aşağıdaki C programında

a) sentaks hatası var.
b) tanımsız davranış (undefined behavior) var.
c) derleyiciye bağlı bir(implementation defined/specified) durum var.
d) Bir sorun yok, ekrana şu değeri yazar:

/********************** kod ******************************/

#include <stdio.h>

int main()
{
	int n = 3;
	int i = 0;

	switch (n % 2) {
	case 0:
	do {
			++i;
	case 1: ++i;
	} while (--n > 0);}

	printf("%d\n", i);
}
/********************** kod ******************************/
